# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

ChandruAI is a new project. This file will grow as the codebase is established.

## Getting Started

No build system or dependencies have been configured yet. Update this section once a stack is chosen (e.g. `npm install`, `pip install -r requirements.txt`, etc.).

## Architecture

To be documented as the project takes shape.
